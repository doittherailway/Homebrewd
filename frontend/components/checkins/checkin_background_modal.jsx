import React from 'react';

const CheckinBackgroundModal = (props) => {
    return(
        <div className="modal-bg-comp" onClick={props.handleClose}>
        </div>
    )
};

export default CheckinBackgroundModal;